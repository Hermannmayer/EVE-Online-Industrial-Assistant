# 三页审计报告：工业制造 / 市场贸易 / 合同市场

> 审计日期: 2026-09-26 | 审计依据: 用户指令「对于工业制造、市场贸易和合同市场页面进行功能、架构、性能的审计」
> 代码基线: `main` = `c7146f8`（工作树干净）
> 审计方式: **只读代码审查 + 对真实 `database/*.db` 的只读测量**；未修改任何业务代码
> 交付约束: 初版为**仅报告，不应用修复**；用户随后指示「修正这些」→ 已按第 6 节顺序修复，**修复状态见第 7 节**
> 数据规模: `market_prices` 84,967 行 / `market_volume_snapshots` 638,837 行 / `public_contracts` 35,000 行 / `contract_items` 41,686 行 / `user.db` 3 条计划、2 个机库

---

## 0. 总评

三个页面的**分层与数据流是清楚的**：QML 只转发、桥只做状态管理、算术集中在 `domain/contract_analysis` 与 `domain/scoring`，SQL 全参数化，动态拼接处只用闭集白名单。合同页把「唯一写库入口互斥」「QThread 不能随宿主析构」这两条踩过的坑都写进了代码并加了防线（`contract_workers._LIVE_WORKERS`）。工业页计划表的逐格重扫缺陷已修（`_view_cache`），`scoring_facade` 的倍率也进了 cache_key。

但**线程生命周期与「改设置后是否真的重算」这两类缺陷只修了一半**：

- 合同页为自己踩过的 `QThread` 析构崩溃加了全局强引用注册表，而**贸易页与工业页的评分线程没有**——同一类崩溃在另两处仍可复现（本报告用探针实测了崩溃机理）。
- 采购汇总那里已经写了「运行中改设置 → 完成回调补算一次」的补丁，而**评分重算路径没有**——这正好是 `docs/dev/flows.md:161` 点名的缺陷类（「改设置数字不动」）。
- 合同页「同步查库不开线程」的设计依据写着 **49 ms**，本次实测同一入口是 **346 ms**（约 7 倍）。设计结论未必错，但依据已过时，而补齐期间每 500 条就要在主线程重跑一次这条 346 ms 的流水线。

**结论：发现 1 项 P0（进程级崩溃）、3 项 P1（功能/健壮性）、5 项 P2（性能/架构/可维护）**，另有 7 处「疑似问题经核对后排除」，一并记录以免后人重复怀疑。

> ⚠️ **初版勘误（2026-09-26，修复阶段自查）**：初版把工业页 `_score_worker` 列为 P0-2「同一类崩溃」，
> 经探针复核**该条是误报** —— `ScoreWorker(..., parent=self)` 带 Qt 父对象，父对象持有它，
> 覆盖属性**不会**析构运行中的线程（探针输出见附录 B）。
> 现已降级为 P2-5 并改写结论；另新增 P2-6（见下）。**该守卫仍保留**，但它修的是另一个真实缺陷：
> 连点两次「添加」会让先提交的那次预览静默不出弹窗（`self._score_worker is not self.sender()` 直接丢结果）。

| 级别 | 数量 | 主题 |
|---|---|---|
| P0 | 1 | 运行中的 `QThread` 被丢失引用 → 进程 hard abort（贸易页排行线程） |
| P1 | 3 | 贸易页排行线程异常未捕获（永久禁用 + 全局崩溃弹窗）· 工业页改价格设置在重算中被静默丢弃 · 合同页物品明细结果串合同 |
| P2 | 5 | 合同页主线程 346 ms × 补齐期间数十次 · 挂单变化全表扫 63.8 万行 · 三条写库路径未统一串行化 · 工业页重复提交丢预览 · 桥读模型私有字段等可维护项 |

---

## 1. 审计范围与方法

### 覆盖范围

| 页面 | 入口 | 审计过的链路 |
|---|---|---|
| 工业制造 | `ui_qml/qml/pages/IndustryPage.qml` | 计划加载 → 缺料标注 → 批量重算 → 采购汇总 → 价格定向刷新 → 甘特 → 下线/启动入口 → 工具窗 |
| 市场贸易 | `ui_qml/qml/pages/TradePage.qml` | 跨区域价差 SQL → 挂单变化 → 内存筛选 → 排序 → 状态栏时效 → 购物车 |
| 合同市场 | `ui_qml/qml/pages/ContractPage.qml` | 列表查询 → 页签计数 → 物品补齐 → 发布者解析 → 三类合同估值 → 运输跳数 → 详情面板 |

### 方法

1. 逐行读 UI 层（`qml/pages/`、`bridge/`、`workers/`、`views/`）与对应 `services/*`；
2. 用 `EXPLAIN QUERY PLAN` + 计时脚本对准真实 `database/*.db` 做**只读**测量；
3. 用独立探针验证「运行中的 `QThread` 丢引用」的实际后果；
4. 拿仓库自己的文档（`docs/dev/flows.md`、`CLAUDE.md` 的缺陷分类）作为对照，判断某项是「已知并已修」还是「同类未覆盖」。

### 未覆盖（明确声明）

- **未跑测试套件**：本次是只读审计、未改代码。上一轮 `-m ui` 的结果是 914 passed / 265.88s。
- **未做真机交互**：所有数字来自离屏脚本对真实库的只读测量，不是点了界面看出来的。
- **工业页的性能结论是推断而非实测**：本机 `user.db` 只有 3 条计划、2 个机库（`load_plans` 14 ms、`get_hangar_stock` 0 ms）。凡涉及「计划数一多就……」的，报告里都标了「未在本数据量下验证」。

---

## 2. P0 — 进程级崩溃

> 初版的 P0-2（工业页 `_score_worker`）经复核为**误报**，已移出本节 → 见 P2-5。

### P0-1 贸易页：运行中的排行线程可能被析构 → 进程 hard abort

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/workers/trade_workers.py:31-45`、`ui_qml/bridge/trade_bridge.py:240-257`、`ui_qml/bridge/trade_bridge.py:374-384`、`ui_qml/qml/pages/TradePage.qml:64-125` |
| 触发 | 点「开始计算」→ 在结果回来前改动任一参数下拉（起点/终点/买卖侧/⇄）→ 再点「开始计算」 |
| 影响 | 进程直接 abort（`0xC0000409`），无 Python traceback、无崩溃弹窗，用户看到的是「窗口突然没了」 |

**证据链**

- `trade_bridge.py:255-257`：

  ```python
  self._worker = worker
  worker.finished_signal.connect(lambda rows: self._on_rank(gen, rows))
  worker.start()
  ```

  `worker` 是 `parent=None` 的 `QThread`（`trade_workers.py:23` `super().__init__(parent)`，调用处不传 parent），`self._worker` 是**唯一**强引用；`_start_rank` 里的赋值会立刻把上一个线程的引用计数打到 0。

- `trade_bridge.py:374-384` `_invalidate()` 把 `_busy` 置回 `False`，但**不等待也不保留**在跑的旧线程：

  ```python
  self._gen += 1
  self._busy = False
  ```

  紧接着 `analyze()`（`trade_bridge.py:232` `if self._busy: return`）就会放行，于是走进上面那次覆盖赋值。

- `TradePage.qml:64-115`：四个参数下拉**在计算中并不禁用**（只有 `analyzeButton` 是 `enabled: !page.trade.busy`），所以用户完全能在计算途中改参数。

- 排行一次耗时约 **240 ms**（实测：价差 78 ms + 挂单变化 158 ms，见附录 A），即上面这条路径有一个 0.2~0.3 s 的真实窗口。

**崩溃机理已实测**（探针脚本见附录 B）：

```
QThread: Destroyed while thread '' is still running
exit=-1073740791        # 0xC0000409
```

这与仓库自己记录的失败模式完全一致 —— `ui_qml/workers/contract_workers.py:22-23`：

> `QThread` **不能在运行中被析构**。挂到桥上（`parent=bridge`）就会随宿主一起走，宿主先销毁即崩

合同页为此专门做了模块级强引用注册表（`contract_workers.py:24-32` 的 `_LIVE_WORKERS` + `spawn()`），**贸易页没有走这条路径**，它用的是单个可变属性。

**建议**（按成本从低到高）

1. 最低成本：`_start_rank` 覆盖前先收尾旧线程 —— `old = self._worker; if old is not None and old.isRunning(): old.wait()`（排行 240 ms，等一下无感）。
2. 与合同页对齐：改用 `contract_workers` 同款的强引用注册表（可抽成 `ui_qml/workers/lifecycle.py` 已有模块的通用 `spawn`），从机制上消除这类问题。
3. 顺带：`TradePage.qml` 的四个参数下拉在 `busy` 时禁用（`_invalidate` 的语义本来就该是「旧结果作废」，不该同时把「能不能再点」也放开）。

### ~~P0-2 工业页：`_score_worker`~~ → 已降级为 P2-5（初版误报）

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/views/industry_view.py:689-691`（初版写作 654-672）、`775-776` |
| 初版结论 | 「同 P0-1 的析构崩溃（同一机理，另一处未加防线）」 |
| **复核结论** | **误报，不会崩溃。** `ScoreWorker(..., parent=self)` 带 Qt 父对象，**父对象持有子对象**，覆盖属性不会析构运行中的线程；本类还另有 `shutdown()`（`industry_view.py:155-189`）显式等它收尾 |

**为什么初版错了**：P0-1 成立的前提是 `parent=None`（无 Qt 父 → 只剩 Python 属性一个引用）。
`trade_workers.CrossRegionRankWorker(password=None)` 确实如此；而工业页评分 worker 传了 `parent=self`，
初版直接套用了前者的机理，没有区分这两种情况。

**复核证据**（探针 `audit_probe5.py`，与 `ScoreWorker(..., parent=self)` 同形）：

```
== 带 parent 的 QThread：丢引用 + gc ==
  引用已丢弃，等 2.5 秒
  [worker] run() 结束
== 存活：带 parent 的 QThread 不会被提前析构 ==
事件循环正常退出
exit=0
```

对照无 parent 的同形探针（附录 B）：`QThread: Destroyed while thread '' is still running` / `exit=-1073740791`。

**该处仍有一个真实缺陷（降级为 P2-5）**：连点两次「添加」时第二次会覆盖 `self._score_worker`，
第一次的结果回到 `_on_score` 时被 `if self._score_worker is not self.sender(): return` 丢掉
（`industry_view.py:706-707`）—— **第一次提交的预览弹窗永远不出现，也没有任何提示**。
表现为「点了回车什么都没发生」。建议与其它四个 worker 保持一致的守卫（当前已按此修复并给出提示）。

---

## 3. P1 — 功能与健壮性

### P1-1 贸易页：排行线程异常未捕获 → 按钮永久禁用 + 误报「程序崩溃」

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/workers/trade_workers.py:31-45`、`ui_qml/bridge/trade_bridge.py:229-264`、`ui_qml/qml/pages/TradePage.qml:118-125` |
| 触发 | `fetch_cross_region_spread` / `fetch_hub_order_change` 抛异常（最现实的一种：顶栏「更新价格」正在写 `market.db`，本页同时读同一文件 → `sqlite3.OperationalError: database is locked`） |
| 影响 | ① `finished_signal` 永不发射 → `_busy` 永远为 `True` → 「开始计算」永久禁用，只能重开页面；② 逃逸异常走全局钩子 → 弹出「程序遇到了意外错误，请重启应用」 |

**证据**

- `trade_workers.py:31-45` 的 `run()` **没有任何 try/except**：

  ```python
  def run(self):
      rows = fetch_cross_region_spread(...)
      change = fetch_hub_order_change(self._region_b, days=self._change_days)
      ...
      self.finished_signal.emit(rows)
  ```

  对照合同页三个 worker（`contract_workers.py:44-53`、`87-113`、`127-138`）**每个都包了 try/except 并 emit 失败**——贸易页是三个页面里唯一的例外。

- `trade_bridge.py:236-238` 置 `self._busy = True`，唯一复位点在 `_on_rank`（`262`）。异常时 `_on_rank` 不会被调用，`_busy` 卡死。

- `core/diagnostics.py:93` 明确写了 PySide6 的行为：「`QThread.run()` 逃逸异常实际上会走 `sys.excepthook`」；而 `Main.py:217` 把它指向 `_global_exception_handler`，后者 `Main.py:150-158` 会弹模态 `QMessageBox`「程序遇到了意外错误，请重启应用」。

  也就是说：**一次可恢复的数据库锁冲突，用户看到的是「程序崩溃了，请重启」**。

- 触发条件并不罕见：`docs/dev/flows.md:36` 记录「全库唯一的取价入口是主工具栏右上角的『更新价格』」，那条通道会写 `market.db`；本页只读同一文件，两者可以同时进行。

**建议**

1. `run()` 包 try/except，失败时 emit 一个带错误信息的结果（或独立的 `failed_signal`），桥里复位 `_busy` 并把原因写进 `statusText`；
2. 防御性兜底：`analyze()` 里加「`self._worker` 已结束但 `_busy` 仍为 True → 复位」的自愈判断；
3. 可选：与合同页一样，在状态栏如实说明「读库失败：database is locked，请等价格更新完成」。

### P1-2 工业页：批量重算在跑时改价格设置会被静默丢弃 → 数字不动

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/bridge/industry_bridge.py:203-216`、`ui_qml/views/industry_view.py:421-436`、`ui_qml/views/industry_view.py:462-546` |
| 触发 | 计划表上有活跃计划时改「材料/成品」的 Hub / 价格类型 / 倍率，且改的瞬间批量重算线程正在跑 |
| 影响 | 表里的成本/利润/利润率仍是**旧价格口径**算出来的值，且不会自己恢复；用户理解成「改设置没用」 |

**证据（时序）**

1. `industry_bridge.py:214-216`：`save_settings(...)` → `priceSettingsChanged.emit()` → `self._page.load_plans()`。
2. `load_plans`（`industry_view.py:292-330`）末尾调 `_auto_calculate_plans(rows)`。
3. `industry_view.py:427-428`：

   ```python
   if self._recalc_worker and self._recalc_worker.isRunning():
       return
   ```

   → **本次改设置被直接丢掉，没有任何标记**。

4. 旧线程随后完成，`_on_recalc_done`（`462-546`）把**按旧口径**算出的结果写库（`538` `update_batch`），然后在 `505-506` 置 `self._recalc_busy = True`，再调 `load_plans()`（`539`）—— 而这次 `load_plans` 里的 `_auto_calculate_plans` 会在 `424-425` 因 `_recalc_busy` 为真而返回，最后才在 `545-546` 的 `finally` 复位 `_recalc_busy`。

   于是「用新口径重算」这个动作**在该轮里彻底没有发生**。

5. 之后也没有别的路径会补上：30 s 心跳（`128-150` `_on_countdown_tick`）只在有过期计划时才 `load_plans`，否则只跑 `_refresh_material_status`（只刷缺料标注）；切页回来的 `on_shown`（`188-198`）只 `reloadPriceSettings()` —— 它只发信号让 QML 控件同步，**不查库也不算**。

**这不是新缺陷类**，仓库自己已经点名过，并在采购汇总那条链上修过：

- `docs/dev/flows.md:161`：「消费方必须**整套一起透传**（漏一项就是「改设置数字不动」的缺陷）」；
- 同一份文件 `165` 记录采购指纹必须含价格口径；
- 而 `industry_view.py:408-419` 的 `_on_procurement_summary_done` **已经实现了正确的补算模式**：

  > 算的这段时间里价格设置又变了 → 本次结果已过期，用同一批计划补算一次

  评分路径缺的就是这十几行。

**建议**：照抄采购汇总的既有模式 —— 在某处记下「口径已变」（例如 `_recalc_dirty = True`），`_on_recalc_done` 的 `finally` 复位 `_recalc_busy` 之后，若该标记为真则用新设置重跑一次；`_price_fp()`（`332-344`）已经把价格口径打包成元组，可直接复用做「口径是否变了」的判据。

### P1-3 合同页：物品明细结果可能串到另一份合同上

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/bridge/contract_bridge.py:565-579`、`ui_qml/bridge/contract_bridge.py:414-431`、`ui_qml/bridge/contract_bridge.py:527-531` |
| 触发 | 快速点两份不同合同；或在「补齐全部」进行中点了某一行 |
| 影响 | 下方物品表显示的是**上一份合同**的物品与价差，而上方选中的是另一份 —— 数字张冠李戴，且看不出错 |

**证据**

- `contract_bridge.py:568-575`：每次选择都新建一个 `ContractItemsLoadWorker`，赋值给 `self._items_worker`（覆盖上一个）。
- `contract_bridge.py:577-579` 的回调**没有任何身份校验**：

  ```python
  def _on_items_loaded(self, items: list) -> None:
      self._item_model.set_rows(items)
      self.itemsChanged.emit()
  ```

  既没有 `if self._items_worker is not self.sender(): return`，也没有把 `contract_id` 带回来比对。**同一个文件里其它异步回调都做了 sender 守卫**（`_on_procurement_summary_done` 之于工业页是同一套写法；本文件 `_on_fetch_done`/`_on_fill_done` 靠 `_busy_worker` 语义隔离）——这里是唯一漏掉的。

  两个线程都可能往同一个槽发结果，**谁后到谁赢**，即使它属于更早的那次选择。

- 触发面比「手快」更宽：`_restore_selection`（`414-431`）在**每次** `_reload_rows` 后，只要还有选中行就再起一个 items worker（`431`）；而补齐过程中 `_on_fill_progress`（`527-531`）每 500 条就 `_reload_rows()` 一次。也就是说补齐期间会**反复**给同一份合同起新的明细线程，彼此竞争同一个回调。

**建议**：把 `contract_id` 一起发回来，回调里比对 `self._selected_contract` 的 id，不一致就丢弃；顺手补上 sender 守卫（与文件内其它回调一致）。

---

## 4. P2 — 性能、架构与可维护性

### P2-1 合同页：`_reload_rows` 实测 346 ms 跑在主线程，补齐期间每 500 条跑一次

**实测（真实库，物品交换页签，3000 行上限）**

| 入口 | 实测 | 代码注释里的说法 |
|---|---|---|
| `load_exchange_contracts` | **346 ms**（3000 行） | `contract_bridge.py:347`「最重的一档（物品交换 3000 行）本机 **49 ms**」 |
| `load_auction_contracts` | 53 ms（229 行） | 「其余 1~4 ms」 |
| `load_courier_contracts("shortest")` | 44 ms（89 行） | — |
| `count_tab` × 3 | 23 ms | — |

`contract_bridge.py:344-356` 的结论是「**同步，刻意不开线程**」，理由正是那个 49 ms：

> 本地查询这么快，线程只换来一类崩溃风险，不值。

结论未必错（那句注释同时记录了异步导致的 `access violation` 实测，理由独立成立），但**依据已经差 7 倍**，而这个数字是设计决策的支点。按当前实测：

- 一次页签切换/筛选应用/换星域 ≈ `_refresh_tab_counts` 23 ms + `_reload_rows` 346 ms ≈ **370 ms 主线程冻结**；
- 补齐期间 `_on_fill_progress`（`530-531`）每 500 条回灌一次：一个星域 3.4 万份合同 → 最多约 68 次 × 346 ms ≈ **累计 23 s 的界面无响应**（分散成 68 次卡顿）。

**建议**

1. 回灌节流：把「每 500 条重播一次」改成按时间（如每 1~2 s 最多一次），或补齐期间只重排/刷新可见行而不重跑完整流水线（`_reload_rows` 的 items/price 段是主要成本）；
2. 把 `_reload_rows` 的时延注释按实测更新（顺带 `docs/dev/flows.md` 里同源的性能叙述）；
3. 若实测在更大库上继续变差，再把 items/price 段移回线程（此时 P1-3 的身份校验必须先做掉，否则竞态会放大）。

### P2-2 贸易页挂单变化：`market_volume_snapshots` 63.8 万行全表扫

| 项 | 内容 |
|---|---|
| 位置 | `services/market_browser_service.py:220-231`（`_ORDER_CHANGE_SQL`）、写入与保留见 `services/importers/getprices.py:259-266` |
| 实测 | `fetch_hub_order_change` **158~232 ms**（两次测量），返回 15,657 组 |
| 性质 | 该查询在 worker 线程（不卡 UI），但耗时随快照累积**线性增长** |

**证据**：`PRAGMA index_list('market_volume_snapshots')` 只有 `sqlite_autoindex_..._1`（PK `(type_id, region_id, date)`），**leading 列是 `type_id`**；而 CTE 的谓词是 `WHERE region_id = ? AND date >= date('now', ?)`。`EXPLAIN QUERY PLAN` 的结果与此一致：

```
SCAN market_volume_snapshots USING COVERING INDEX sqlite_autoindex_market_volume_snapshots_1
```

即每算一次挂单变化都扫全部 63.8 万行（当前只有 20 个不同日期，保留窗口 90 天，会继续涨）。

**建议**：加 `market_volume_snapshots(region_id, date)` 索引。按 `CLAUDE.md` 的克制条款第 3 条，market 库是可重建缓存、加列走 `DB_SCHEMA_VERSIONS` + `_MIGRATIONS`；索引同理（`services/schema_migrations.py:29` 的 `mkt` 那一档已有先例：`64-71` 就是给 `market_prices(fetch_time)` 加索引，同一手法可复用）。

### P2-3 三条写 `market.db` 的路径没有统一串行化

- 顶栏「更新价格」有自己的单写者排队（`ui_qml/shell_window.py:345`：「排队等着的下一轮 —— 串行化，避免两处同时写 market.db / 撞 ESI 限流」）；
- 合同页在**页面内部**做了互斥（`contract_bridge.py:7-8` 的 `_busy_worker`，拉列表与补物品二选一），这是好的；
- 但**跨页没有互斥**：合同页的 `ContractFetchWorker`/`ContractFillWorker`（`contract_workers.py:44-113`，各自 `aiosqlite.connect` 写 `public_contracts`/`contract_items`/`contract_issuers`）与顶栏的价格更新可以同时进行，落在同一个 SQLite 文件上。

SQLite 是单写者，两者相撞就退化为 `database is locked` ——（a）合同补齐会弹失败文案、（b）**配合 P1-1，贸易页读同一文件也会连带撞上**。

**建议**：把合同页的两个写任务也挂到外壳那条写队列上（或至少让状态栏提示「价格更新进行中，合同写入已排队」）。属于架构取舍，不急于动手。

### P2-5 工业页：连点「添加」时先提交的那次预览会被静默丢弃（原 P0-2 降级）

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/views/industry_view.py:689-691`、`706-707`、`775-776` |
| 触发 | 第一次评分尚未结束时再次提交（双击回车 / 连点「添加」） |
| 影响 | 第一次提交的结果被 `if self._score_worker is not self.sender(): return` 丢掉 → **预览弹窗永远不出现，也没有任何提示**（表现为「点了回车什么都没发生」）；不崩溃 |

**建议**：赋值前加 `isRunning()` 守卫，并给用户一句「正在计算预览，请稍候」（已按此修复）。

### P2-6 合同页：切换合同时旧物品仍留在表里（与 P1-3 同源的显示窗口）

| 项 | 内容 |
|---|---|
| 位置 | `ui_qml/bridge/contract_bridge.py:560-593` |
| 触发 | 点完一份合同立刻点另一份（旧结果被丢弃的那段窗口内） |
| 影响 | 丢弃旧结果只解决了「结果串号」，但**表里那几行仍是上一份合同的**，直到新结果回来才替换 —— 中间这段用户读到的价差属于别的合同 |

**建议**：`selectContract` 里先清空物品表，再做异步加载（已按此修复）。

### P2-4 其它可维护项

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| 1 | `ui_qml/bridge/contract_bridge.py:583, 639, 656` | 桥直接读模型的私有字段 `self._item_model._rows`（`itemSummary` / `copyItems` / `addItemsToWatchlist` 三处），与「桥只转发」的约定不符 | 给 `ContractItemTableModel` 加只读 `rows()` 访问器（模型已有 `row_at` 先例） |
| 2 | `ui_qml/bridge/industry_bridge.py:246-257` | `update_stats` 在**桥**里算 running/pending/mats_lines 统计口径（不是转发），既难测也容易和 `plan_aggregator` 的口径分叉 | 下沉到 service（或 `ui_data_service`）成一个纯函数；按克制条款不必现在做 |
| 3 | `ui_qml/views/industry_view.py`（889 行） | `IndustryPage` 一个类同时管计划加载、缺料判定、批量重算、采购汇总、甘特、五个弹窗与两个工具窗 | 属批次 7.4 的过渡形态（`CLAUDE.md` 已把 `views/` 定性为残留）。**不建议现在拆**，只记录 |
| 4 | `services/logistics.py:73-75` | `_JUMP_CACHE` 无上限，按 `(origin, destination, mode, min_security)` 累积；合同页可以产生大量不同星域对 | 需要时换 `core/cache.TtlLRUCache`（现已有该工具） |
| 5 | `ui_qml/bridge/trade_bridge.py:266-294` | `_status_text()` 每次都同步查一次 `fetch_hub_fetch_time`（实测 **18 ms**），而它被 `_apply_filters` 调用 —— 每切一次筛选项就查一次库 | 量级不大；若后来变慢，把时效缓存到上次计算完成时 |
| 6 | `services/logistics.py:9-11` 与 `docs/dev/flows.md:104-118` | 两处写「物流运费**当前无 UI 调用方**」，但 `compute_jumps` 实际被合同运输页每行调用（`services/contract_service.py:381`） | 措辞容易误导（flows.md 已在同段补了说明）；建议把「无调用方」限定为 `estimate_freight_cost`/`calc_transport_profit` |

---

## 5. 已核对并排除的疑点

以下 7 项在审计中都被怀疑过，核对后**判定为非缺陷**，记录在此避免重复排查：

1. **合同页 `on_shown` 自动补齐是否接线到位** —— 是。`ui_qml/registry.py:148` 把 bridge 作为 `PageSpec.hooks`，`ui_qml/shell_window.py:705, 711-713` 按名 `getattr(hooks, "on_shown")` 调用。不是死代码。
2. **合同页「同步查库不开线程」是不是偷懒** —— 不是。`contract_bridge.py:344-356` 记录了一次实际发生的 `access violation`（异步结果回来时视图正在销毁）。问题只在时延数字过时（见 P2-1）。
3. **计划表逐格重扫全表** —— 已修。`_view_cache` 按签名失效，`docs/dev/flows.md:153-159` 有回归防线 `tests/test_qml_plan_model.py::test_cell_reads_do_not_rescan_the_whole_table`。当前 `data()` 走缓存。
4. **`scoring_facade` 缓存是否漏了倍率** —— 没漏。`docs/dev/flows.md:163` 记录 `mat_price_mult/prod_price_mult` 必须进 cache_key，代码已如此。
5. **采购汇总的自制件集合口径** —— 正确。`industry_view.py:358` 按**全量** `rows` 算 `self_made`（不是筛过的 `procur`），与 `docs/dev/flows.md:166-173` 的修复一致；未复现「子线被重复计成待采购」。
6. **SQL 注入面** —— 干净。三页所有用户输入都走参数化（`contract_service._build_where:68-99`、`market_browser_service.search_items:59-75`）；动态拼接仅限列名白名单（`market_browser_service.py:82-86` 的 `_PRICE_COLS`/`_VOL_COLS`）与 `IN` 占位符，均不接触用户输入。
7. **`contract_issuers` 无索引导致按发布者查很慢** —— 实测不是问题。该表 `issuer_id INTEGER PRIMARY KEY`（rowid），`EXPLAIN QUERY PLAN` 给出 `SEARCH contract_issuers USING INTEGER PRIMARY KEY`；`name LIKE` 那侧是全扫，但表只有 1,828 行。合同页最重的一档（19 ms 计数 / 35 ms 取页）**不是**这条。

---

## 6. 建议整改顺序

1. **P0-1**：排行线程引用守卫。改动极小（几行），消除的是进程级崩溃。
2. **P1-1**：贸易 worker 加异常处理。改动小，消除的是「永久禁用 + 误报崩溃」。
3. **P1-2**：工业页补算标记。照抄 `_on_procurement_summary_done` 的既有模式，可复用 `_price_fp()` 做判据。
4. **P1-3**：合同页 items 回调带 `contract_id` 校验 + sender 守卫。
5. **P2-1 / P2-2**：先做 P2-2（加索引，一次迁移就够，收益确定），再做 P2-1 的回灌节流。
6. **P2-5 / P2-6**：两处体验缺陷，各几行，顺手修。
7. **P2-3 / P2-4**：架构取舍与可维护项，按克制条款不必现在动。

> 测试口径提醒（`CLAUDE.md`）：P0-1/P2-5 属「修 bug → 1 条最小回归」；P1-1/P1-2/P1-3 若改动落在 `ui_qml/` 且涉及 QThread，按 `shell_snapshot.py` + `ui-retest` 一档跑一次即可；P2-2 属 schema 迁移 → `validate` 一次。**不要逐级升档。**

---

## 7. 修复状态（2026-09-26，用户指令「修正这些」后）

### 已修

| # | 修复内容 | 落地位置 | 回归测试 |
|---|---|---|---|
| P0-1 | 排行线程改走模块级强引用注册表 `spawn()`（与合同页同一手法），`_LIVE_WORKERS` 在 `finished` 时自摘 | `ui_qml/workers/trade_workers.py:9-17`；`ui_qml/bridge/trade_bridge.py:240-260` | `test_failed_rank_clears_busy_and_explains`（同文件，覆盖失败分支） |
| P1-1 | `run()` 包 try/except，新增 `failed_signal`；桥在失败时复位 `_busy` 并给可恢复文案 | `ui_qml/workers/trade_workers.py:44-62`；`ui_qml/bridge/trade_bridge.py:269-275` | `tests/test_qml_trade.py::test_failed_rank_clears_busy_and_explains` |
| P1-2 | 新增 `_recalc_settings_fp()` 口径指纹 + `_recalc_dirty` 标记；`_on_recalc_done` 落库并解锁后按新口径补跑一轮 | `ui_qml/views/industry_view.py:424-448, 466-468, 528, 570-573` | `tests/test_ui_industry.py::test_recalc_reruns_when_price_settings_change_mid_flight` |
| P1-3 | 明细回调改带 `contract_id` + worker 身份双重校验 | `ui_qml/bridge/contract_bridge.py:585-593` | `tests/test_qml_contract.py::test_stale_items_result_is_dropped` |
| P2-1 | 补齐回灌按时间节流（`≥1.0 s` 才重播一次列表），每轮补齐重置窗口 | `ui_qml/bridge/contract_bridge.py:74, 521, 535-537` | 由既有补齐用例兜底（纯时序节流，按判定表不新增用例） |
| P2-2 | 注册 `mkt` v3→v4：`market_volume_snapshots(region_id, date)` 索引 | `services/schema_migrations.py:29, 76-90, 625` | `tests/test_schema_migrations.py::test_mkt_v3_to_v4_creates_snapshot_region_date_index` |
| P2-5 | 评分线程加 `isRunning()` 守卫 + 「正在计算预览，请稍候」提示 | `ui_qml/views/industry_view.py:689-691` | 由既有用例兜底（守卫分支，不新增用例） |
| P2-6 | `selectContract` 先清空物品表，再异步加载 | `ui_qml/bridge/contract_bridge.py:569-572` | 同上（显示窗口，不新增用例） |

**连带修正**：

- `tests/conftest.py`：market.db 夹具 `PRAGMA user_version` 2 → 4，与 `DB_SCHEMA_VERSIONS["mkt"]` 同步（`docs/dev/schema-migration.md` 第 84 行的既有约定）。
- `tests/test_qml_trade.py`：`_FakeWorker` 补 `finished` / `failed_signal` —— 真实 `QThread` 必有这两个接口，桥的 `spawn()` 会连它们。
- `tests/test_schema_migrations.py`：三处 `after == 3` / `v == 3` 期望随版本号上移到 4。

### 未修（按克制条款留待决策）

- **P2-3**（三条写库路径未统一串行化）：架构取舍，改动面大，未动。
- **P2-4**（桥读模型私有字段、`update_stats` 口径下沉、`IndustryPage` 拆分、`_JUMP_CACHE` 上限、状态栏同步查询、文档措辞）：均为可维护性建议，未动。

### 验证

```
pytest tests/test_schema_migrations.py tests/test_contract_service.py tests/test_contract_models.py -q
→ 107 passed in 7.31s

pytest tests/test_qml_trade.py tests/test_qml_contract.py tests/test_ui_industry.py -q
→ 78 passed in 26.22s（含上述 3 条新增回归）
```

> ⚠️ 跑这批 UI 用例时**不要手动设 `QT_QPA_PLATFORM`**：仓库 conftest 自己配置 Qt 后端，
> 覆盖成 `offscreen` 会让 `PageHost` 加载页签时挂死（实测卡满 `faulthandler` 的 120 s 超时）。
> 直接 `pytest <files>` 即可（本次即因此误判过一次「改动引入挂起」）。


---

## 附录 A：实测数据（只读）

环境：Windows，`.venv/Scripts/python.exe`，真实 `database/*.db`（测量脚本自身设 `QT_QPA_PLATFORM=offscreen`，
**只影响探针脚本**；跑 pytest 时不要设这个变量，见第 7 节末尾的提醒）。

**库规模**

| 项 | 值 |
|---|---|
| `market_prices` | 84,967 行 / 5 个 region |
| `market_volume_snapshots` | 638,837 行 / 20 个不同 date（2026-07-27 ~ 2026-09-26） |
| `public_contracts` | 35,000 行（全部在 10000002） |
| `contract_items` | 41,686 行 |
| `contract_issuers` | 1,828 行 |
| `market.db` / `reference.db` 体积 | 63.3 MB / 22.2 MB |

**合同页**

| 入口 | 行数 | 耗时 |
|---|---|---|
| `load_exchange_contracts` | 3000 | **346 ms** |
| `load_auction_contracts` | 229 | 53 ms |
| `load_courier_contracts("none")` | 89 | 7 ms |
| `load_courier_contracts("shortest")` | 89 | 44 ms |
| `count_tab` × 3 个页签 | — | 23 ms |
| 合同计数 `COUNT(*)` | 29,237 | 19 ms |
| 合同取页 `ORDER BY … LIMIT 3000` | — | 35 ms |

**贸易页**

| 入口 | 行数 | 耗时 | 所在线程 |
|---|---|---|---|
| `fetch_cross_region_spread` | 11,544 | 78 ms | worker |
| `fetch_hub_order_change`（7 天） | 15,657 | 158 ms（另一次测 232 ms） | worker |
| worker 合计（含合并） | — | ~240 ms | worker |
| `_apply_filters` 一次 | 279（筛后） | 3 ms | **主线程** |
| `TradeRankQmlModel.set_rows` | 11,544 | 6~8 ms | **主线程** |
| `fetch_hub_fetch_time` | — | 18 ms | **主线程** |

**索引与执行计划**

```
# market_volume_snapshots：只有 PK(type_id, region_id, date)
SCAN market_volume_snapshots USING COVERING INDEX sqlite_autoindex_market_volume_snapshots_1

# 跨区域价差：region_id 不是 market_prices PK 的 leading 列
SCAN a
SEARCH b USING INDEX sqlite_autoindex_market_prices_1 (type_id=? AND region_id=?)

# contract_issuers：issuer_id 是 rowid 主键
SEARCH contract_issuers USING INTEGER PRIMARY KEY (rowid=?)
```

**工业页（本数据量下未见性能问题）**

| 入口 | 耗时 |
|---|---|
| `plan_service.load_plans("全部")` | 14 ms（3 条计划） |
| `plan_aggregator.self_made_type_ids` | 4 ms |
| `inventory_manager.get_hangar_stock` × 2 | 0 ms |

---

## 附录 B：复现方式

**P0 的崩溃机理**（独立探针，`parent=None` 的 QThread，启动后丢弃最后一个引用）：

```python
w = Slow()  # run() 里 sleep(2)
w.start()
del w
gc.collect()  # 实际代码里连 gc 都不需要：覆盖赋值即触发 refcount 归零
```

输出：

```
QThread: Destroyed while thread '' is still running
exit=-1073740791
```

**同一探针的反例（P0-2 误报的依据）** —— 只把 `Slow(self)` 改成带 Qt 父对象，其余不变：

```
== 带 parent 的 QThread：丢引用 + gc ==
  引用已丢弃，等 2.5 秒
  [worker] run() 结束
== 存活：带 parent 的 QThread 不会被提前析构 ==
事件循环正常退出
exit=0
```

→ **判据：`parent=None` 的 QThread 只有 Python 属性一个引用，覆盖即崩；带 parent 的由父对象持有，覆盖不会崩。**
看某个 worker 有没有这个风险，先看它构造时传没传 parent，再看持有它的属性会不会被覆盖。

**测量脚本**：本次审计用的取数探针写在 `%TEMP%`（`audit_probe*.py`），**未留在仓库里**，符合 `CLAUDE.md`「别把探针留在仓库里」的要求。需要重测时可照附录 A 的入口直接调对应的 `services/*` 函数或对库执行同样的 `EXPLAIN QUERY PLAN`。
