# EVE Online 商人助手 — 占位函数与未实现功能审计报告

> 扫描时间: 2026-07-04 | 覆盖: 50 个 Python 文件

---

## 一、UI 层 — 生产计划模块（industry）

### 1.1 plan_table.py — 右键菜单占位

| 行号 | 函数/位置 | 当前实现 | 应该做什么 |
|------|-----------|----------|------------|
| L189 | 右键菜单 →「查看核算」 | `lambda: print("查看核算: 功能开发中")` | 打开核算详情弹窗，显示单条计划的成本分解（材料费/安装费/经纪人费/税） |
| L192 | 右键菜单 →「查看物品详情」 | `lambda: print("查看物品详情: 功能开发中")` | 打开物品详情页/弹窗，展示 ESI 物品属性、图标、市场数据 |
| L220 | 右键菜单 →「母项智能调整」 | `lambda: self._phase3_placeholder("母项智能调整")` | 根据市场价/库存自动调整母项计划的流程数/并行数 |
| L222 | 右键菜单 →「子项智能调整」 | `lambda: self._phase3_placeholder("子项智能调整")` | 自动调整子项（材料）的采购量/备料状态 |
| L224 | 右键菜单 →「子项大规模产线并行」 | `lambda: self._phase3_placeholder("子项大规模产线并行")` | 批量设置子项为大规模并行生产模式 |
| L227 | 右键菜单 →「查看原本图的NPC卖家」 | `lambda: self._phase3_placeholder("查看原本图的NPC卖家")` | 查询并显示该蓝图原始 NPC 卖家信息（空间站/价格） |
| L231 | 右键菜单 →「为设施设置所在星系」 | `lambda: self._phase3_placeholder("为设施设置所在星系")` | 为选定设施关联星系 ID，用于 SCI 查询 |
| L233 | 右键菜单 →「为设施所在星系设置成本系数」 | `lambda: self._phase3_placeholder("为设施所在星系设置成本系数")` | 手动设置/覆盖星系成本指数（SCI） |
| L236 | 右键菜单 →「产线启动小助手」 | `lambda: self._phase3_placeholder("产线启动小助手")` | 向导式引导用户启动产线（检查材料/技能/时间） |
| L287-288 | `_phase3_placeholder()` | `QMessageBox.information(self, "功能开发中", ...)` | 统一的阶段三占位方法，所有 Phase 3 菜单项路由至此 |

### 1.2 industry_view.py — 工厂主页面占位

| 行号 | 函数 | 当前实现 | 应该做什么 |
|------|------|----------|------------|
| L267-273 | `_on_predefault()` | 对话框确认后只弹 `QMessageBox` 显示配置值，**不写入数据库** | 将预默认配置（ME/TE/设施/输出/技能）保存到 user 库，作为新建计划的默认值 |
| L275-277 | `_on_plan_add(text)` | `QMessageBox.information(self, "添加计划", f"计划添加功能开发中: {text}")` | 解析用户输入的文本（物品名/蓝图名），创建生产计划并写入 DB |
| L279-281 | `_on_batch_add()` | `QMessageBox.information(self, "批量添加", "从蓝图列表批量添加功能开发中")` | 弹出蓝图选择对话框，批量将选中蓝图加入生产计划表 |

### 1.3 predefault_dialog.py — 整个对话框为阶段一占位

| 行号 | 位置 | 当前实现 | 应该做什么 |
|------|------|----------|------------|
| L29 | `__init__` | `self.setWindowTitle("预默认配置（阶段一占位，功能开发中）")` | 标题应改为正常标题 |
| L79 | 提示标签 | `QLabel("注：此对话框为阶段一占位，部分功能尚未实现。")` | 移除此占位提示 |
| 全文件 | 整体 | 对话框 UI 已完成（ME/TE/设施/输出/技能等级），但 `get_config()` 只返回值，**不执行保存** | 调用方（`_on_predefault`）需要将返回值写入 DB 并应用到计划表 |

---

## 二、UI 层 — 仓库模块（inventory）

### 2.1 inventory_view.py — 蓝图右键菜单占位

| 行号 | 位置 | 当前实现 | 应该做什么 |
|------|------|----------|------------|
| L1859 | 菜单项「研究分析」 | 创建了 `research = menu.addAction("研究分析")`，但 L1886 处统一 fallback | 查询蓝图研究时间/成本，显示研究进度 |
| L1865 | 菜单项「自动填写每流程成本(T2发明)」 | 同上 fallback | 根据发明公式自动计算 T2 蓝图的每流程成本 |
| L1867 | 菜单项「加入制造业规划」 | 同上 fallback | 将选中蓝图快速加入生产计划表 |
| L1871 | 菜单项「加入效率研究规划」 | 同上 fallback | 将选中蓝图加入效率研究计划 |
| L1886-1887 | fallback 分支 | `elif action in (research, auto_cost, add_plan, add_research): QMessageBox.information(self, "提示", "此功能即将上线")` | 四个菜单项全部只弹提示框，无实际逻辑 |

---

## 三、UI 层 — 估算模块（estimate）

### 3.1 estimate_view.py — 精炼功能占位

| 行号 | 位置 | 当前实现 | 应该做什么 |
|------|------|----------|------------|
| L437 | 注释 `# ── 行 2: 精炼占位 ──` | UI 控件已创建但全部 `setEnabled(False)` | 启用精炼功能：一键精炼按钮、人物/设施模式切换、技能预设、气云解压率计算 |
| L568-570 | `_refine_btn` | `QPushButton("一键精炼")` + `setEnabled(False)` + tooltip "即将支持" | 实现精炼计算逻辑并启用按钮 |
| L573-577 | `_char_fac_tabs` | `setEnabled(False)` | 启用人物/设施模式切换 |
| L579-583 | `_skill_preset` | `setEnabled(False)` | 启用技能等级选择 |
| L585-589 | `_gas_rate` | `setEnabled(False)` | 启用气云解压率输入 |
| L595-597 | `_residual_chk` | `setEnabled(False)` | 启用"残余也精炼掉"选项 |

---

## 四、UI 层 — 甘特图模块（gantt）

### 4.1 gantt_view.py — 主题回调空实现

| 行号 | 函数 | 当前实现 | 应该做什么 |
|------|------|----------|------------|
| L121-122 | `_on_theme_changed()` | `pass` | 甘特图使用自定义 `paintEvent`，主题切换时应调用 `self.update()` 触发重绘，或更新内部颜色变量后重绘 |

---

## 五、Services 层

### 5.1 scoring.py — 无占位（已确认）

> 扫描确认：`{...}` 和 `[...]` 出现在 docstring 中（L233-235），是文档中的 JSON 结构示例，**不是**代码占位。scoring.py 已完整实现。

---

## 六、汇总统计

| 分类 | 数量 | 严重程度 |
|------|------|----------|
| 阶段二占位（UI 菜单/方法） | 5 个 | 🟡 中 — 用户可触发但无效果 |
| 阶段三占位（高级功能） | 9 个 | 🟢 低 — 已有统一 placeholder 方法 |
| 整个对话框为占位 | 1 个 | 🟡 中 — 功能可用但不保存 |
| 精炼功能未启用 | 6 个控件 | 🟡 中 — UI 存在但 disabled |
| 主题回调空实现 | 1 个 | 🟢 低 — 不影响功能 |
| 蓝图右键菜单 4 项全部占位 | 4 个 | 🟡 中 — 用户可触发但只弹提示 |
| **合计** | **26 个** | |

### 按优先级排序建议

**P0 — 用户可触发的空功能（最容易被投诉）：**
1. `plan_table.py` L189/192 — 右键「查看核算」「查看物品详情」只 print 到控制台
2. `inventory_view.py` L1886 — 蓝图右键 4 个菜单项全部只弹"即将上线"
3. `industry_view.py` L275/280 — 添加计划和批量添加功能完全为空

**P1 — 核心流程断裂：**
4. `industry_view.py` L267 — 预默认配置不保存，用户配置丢失
5. `estimate_view.py` L568-597 — 精炼栏全部 disabled，估算页面功能不完整

**P2 — Phase 3 高级功能（有统一 placeholder，不影响基础流程）：**
6. `plan_table.py` L218-236 — 7 个智能调整/高级修改菜单项
