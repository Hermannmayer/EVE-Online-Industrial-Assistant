# EVE 商人助手 — 架构概览

> 更新：2026-06-28

---

## 项目定位

一个基于 PySide6（Qt for Python）的桌面应用程序，为 EVE Online 玩家提供多区域市场价格查询、物品搜索、制造利润计算、贸易评分和库存管理功能。

---

## 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| GUI | PySide6 ≥ 6.5 | Qt6 Python 绑定 |
| 数据库 | SQLite3 + aiosqlite | 4 个独立库，异步写入 |
| HTTP | aiohttp | 并发请求 + ESI 限流 |
| 重试 | tenacity | 指数退避 |
| 打包 | PyInstaller | 单可执行文件分发 |
| 日志 | 自封装 `_Logger` | 控制台 + 文件双输出 |
| 工具 | Pillow, pyperclip, tqdm, pyyaml | 图标、剪贴板、进度条、YAML |
| 语言 | Python 3.14 | |
| 格式化 | Ruff | lint + format 合一 |

---

## 项目目录结构

```
EVE-Online-Industrial-Assistant/
├── Main.py                          # 入口点
├── build_release.py                 # 打包脚本
├── dev.py                           # 热重载开发工具
├── README.md
│
├── core/
│   ├── __init__.py
│   ├── paths.py                     # 所有路径集中管理（4 库路径）
│   └── logger.py                    # 日志配置
│
├── ui_pyside6/
│   ├── __init__.py
│   ├── main_window.py               # 主窗口 + 侧边导航 + 弹窗管理
│   ├── theme.py                     # One Dark Pro / One Light 主题 QSS
│   └── views/
│       ├── __init__.py
│       ├── query_view.py            # 物品查询页面（核心功能）
│       ├── industry_view.py         # 工业/制造页面
│       ├── inventory_view.py        # 仓库/库存页面
│       ├── trade_view.py            # 贸易评分页面（占位中）
│       ├── char_settings_view.py    # 角色设置页面
│       ├── init_wizard.py           # 首次启动向导
│       └── all_items_view.py        # 全物品浏览弹窗
│
├── services/
│   ├── client.py                    # ESI HTTP 客户端（基于 aiohttp）
│   ├── database_manager.py          # 多库连接管理器（ATTACH DATABASE）
│   ├── scoring.py                   # 制造/贸易评分计算逻辑
│   ├── scoring_cache.py             # 评分结果缓存
│   ├── inventory_manager.py         # 库存管理 CRUD
│   ├── init_check.py                # 数据初始化状态检测
│   └── workers/
│       ├── getitems.py              # 物品数据库初始化（SDE）
│       ├── getprices.py             # 市场价格拉取（ESI，并发优化）
│       ├── geticon.py               # 图标缓存下载
│       ├── getblueprints.py         # 蓝图数据拉取
│       ├── getindustry.py           # 工业系统成本指数拉取
│       └── getimplantdata.py        # 工业植入体数据拉取
│
├── database/
│   ├── reference.db                 # 静态参考数据（item, industry_* 等）
│   ├── market.db                    # 市场价格数据（market_prices）
│   ├── user.db                      # 用户数据（hangars, inventory 等）
│   └── blueprint.db                 # 蓝图数据（activities, materials 等）
│
├── scripts/
│   └── migrate_split_db.py          # 单库拆分为 4 库的迁移脚本
│
├── data/
│   ├── search_history.json          # 搜索历史
│   ├── update_progress.json         # 更新进度
│   ├── window_geometry.json         # 窗口状态
│   ├── char_config.json             # 角色配置
│   └── caches/icons/                # 图标缓存
│
├── documents/
│   ├── database_split_plan.md       # 数据库拆分方案
│   ├── 界面设计.md                   # 界面设计参考
│   └── EVE_ESI_API.xlsx             # ESI API 参考
│
├── EVE——docs/
│   ├── 01-架构概览.md                # ← 你在这里
│   ├── 02-功能规格.md
│   ├── 03-UI设计.md
│   └── 04-开发路线图.md
│
├── tests/
│   ├── __init__.py
│   ├── test_core.py
│   ├── test_logger.py
│   ├── test_scoring.py
│   └── test_scoring_cache.py
│
├── .claude/
│   ├── CLAUDE.md                    # Claude Code 自动加载的项目说明
│   └── PROJECT.md                   # 项目约定
│
├── pyproject.toml                   # Ruff 和 pytest 配置
└── requirements.txt
```

> **注意：** `EVE——docs/_archive/` 目录存放了旧版 12 篇设计文档（C# 参考设计的技术选型、数据架构、各模块详细设计等），仅供参考，不代表当前实现。

---

## 数据库架构

### 4 库拆分策略

按数据生命周期和变更频率分为 4 个独立 SQLite 文件：

```
database/
├── reference.db   ← 静态参考数据（一次性写入，只读）
├── blueprint.db   ← 蓝图数据（一次性写入，只读）
├── market.db      ← 市场价格（频繁覆写）
└── user.db        ← 用户数据（增删改）
```

### reference.db

| 表 | 内容 |
|----|------|
| `item` | 物品基础信息（type_id, 中/英文名, group, market_group, volume, iconID） |
| `market_tree` | 市场分类树 |
| `industry_system_costs` | 工业系统成本指数 |
| `item_dogma` | 物品 dogma 属性 |

### market.db

| 表 | 内容 |
|----|------|
| `market_prices` | 实时订单价格快照（buy_price, sell_price, buy_volume, sell_volume, fetch_time） |
| `market_volume_snapshots` | 成交量快照 |

### blueprint.db

| 表 | 内容 |
|----|------|
| `blueprint_activities` | 蓝图活动基本信息 |
| `blueprint_materials` | 蓝图材料清单 |
| `blueprint_products` | 蓝图产出 |
| `blueprint_skills` | 蓝图所需技能 |

### user.db

| 表 | 内容 |
|----|------|
| `hangars` | 机库定义（矿仓/组件仓/产品仓等） |
| `inventory_items` | 库存物品（type_id, 数量, 加权平均成本） |
| `user_blueprints` | 用户拥有的蓝图（ME/TE/数量） |
| `production_plans` | 生产计划 |
| `user_skills` | 角色技能 |

跨库查询通过 `ATTACH DATABASE` 实现，详见 `services/database_manager.py`。

---

## 架构模式

扁平架构：**页面 + 后台 Worker 线程 + 直接 SQL**

```
Main.py
  │
  └─ MainWindow (QMainWindow)
       ├── QTreeWidget (侧边导航)
       ├── QStackedWidget (内容区)
       ├── QStatusBar (状态栏)
       └── 后台 QThread (Worker 模式)
            ├── PriceUpdateWorker
            ├── SearchWorker
            ├── OrderFetchWorker
            └── BPMatWorker
```

设计原则：
- 每个 QThread 只做一件事，用完即弃
- 页面之间通过 MainWindow 传参
- 极少抽象层，services/ 下是函数集合，非类

---

## 数据源

| 数据源 | 地址 | 用途 |
|--------|------|------|
| SDE（Static Data Export） | `https://sde.jita.space/latest` | 物品名称、类别、市场分类、图标 |
| ESI（EVE Swagger Interface） | `https://esi.evetech.net/latest` | 市场价格、订单、工业指数 |
| 四大贸易中心 | Jita(10000002), Amarr(10000043), Dodixie(10000032), Rens(10000030) | |
