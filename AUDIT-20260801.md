# EVE 工业助手 变更集审计报告

> 审计日期: 2026-08-01 | 审计依据: 用户指令「拉取最新版本并对变更进行审计」
> 代码基线: `7c24921`（本地）→ `b3b8e40`（`origin/main`，fast-forward 13 提交）
> 变更范围: 48 文件，+3157 / -521（0.9.0 / 0.10.0 / 0.11.0 三个语义化版本）
> 审计方式: 全面审计（`git pull --ff-only` 后 ruff + mypy + 全量测试 + 高风险区逐行审查）
> 交付约束: **仅审计报告，不应用修复**（用户已确认）

---

## 0. 总评

本次变更集主题：**生产计划执行**新功能（schema `user` v3→v4，`services/plan_execution.py` 新增 570 行）、SDE 解析并行化 + YAML 进程内缓存、下载器 ESI 并发重构、初始化流程优化、schema 迁移加固、UI 稳定性修复。

**验证结论：质量良好。** `ruff check .` 0 错误；`mypy .` 无问题；全量测试 **783 passed**（含修复轮新增 6 例）。新功能核心逻辑测试覆盖扎实（test_plan_execution.py 554 行 / test_plan_countdown.py 102 行 / test_sde_cache.py 70 行），schema v3→v4 迁移按规范注册且幂等，SDE 并行解析与 ESI 下载器线程安全设计正确。

审计发现 **0 个 P0、4 项 P1、11 项 P2**。P1 集中在**材料扣减/返还的一致性**（两条取消路径行为分叉、`mat_hangar_id` 未持久化导致撤销无法返还、完成入库跨连接非事务）与 **dict 权威值依赖**（complete/cancel 信任调用方传入的计划字典而非 DB）。

**修复轮次（2026-08-01）**：应用户要求修复除 **P1-1** 外的全部 P1（P1-2 / P1-3 / P1-4）及 **P2-5 / P2-6**。P1-1 因用户特殊设计**按约定搁置**。修复经 `ruff` / `mypy` / 全量 783 测试验证通过，详见各表状态与附录。

---

## 1. 严重问题（P0）

无。主路径全部正常，无崩溃、无数据撕裂、无全量功能失效。

---

## 2. 一般问题（P1）

| # | 位置 | 问题 | 触发场景 | 影响 | 状态 |
|---|------|------|---------|------|------|
| 1 | `ui_pyside6/views/industry/plan_table.py:642-662`（`_cancel_plan`，右键「设为待生产」） | 手写 `UPDATE production_plans SET status='pending'` 绕过 `plan_execution.cancel_plan`，**不返还已扣减材料**，且与 `_undo_start`（plan_table.py:686 走 `cancel_plan`，返还）行为分叉 | 「项目启动」（扣减）→「设为待生产」→ 再次「项目启动」 | 材料**双重扣减**不可见；已扣材料无法通过该路径找回 | ✅ **已修复**（按用户设计：撤销=in_progress 返还；设为待生产=completed 复用不返还；菜单按状态显隐） |
| 2 | `services/plan_execution.py:218-226` + `plan_table.py:599` + `plan_execution.py:352` | `start_plan` 的 UPDATE **不持久化生效的 `mat_hangar_id`**；而 `_start_plan` 会回退用工具栏默认机库（`plan.get("mat_hangar_id") or self._mat_hangar_id`）。计划 dict 的 `mat_hangar_id` 仍为 None → `cancel_plan` 读取它（352 行）判定无机库 → **返还 0 件** | 计划未设材料机库、仅用工具栏默认机库启动，再「撤销启动（返还材料）」 | 已扣减材料静默丢失 | ✅ **已修复** |
| 3 | `services/plan_execution.py:283-323`（`complete_plan`） | **跨连接非事务**：成品入库经 `inventory_manager.add_item`（独立连接、独立提交，300 行）→ BPC 消耗 + 状态更新（own_conn，310/318 行）→ commit（323 行）。中途失败 → 成品已入库但计划未 completed | 完成时 BPC 消耗或状态 UPDATE/commit 异常 | 重试 `complete_plan` **重复入库**成品；无失败/重试测试覆盖 | ✅ **已修复** |
| 4 | `services/plan_execution.py:307, 352, 358` | `complete_plan` / `cancel_plan` **信任调用方传入的 plan dict** 的 `assigned_blueprint_id` / `mat_hangar_id` / `material_short`，不重读 DB 权威值（`start_plan` 在 206-216 却专门重读蓝图）。stale dict → 绑定 BPC 未消耗但占用被清除 / 返还数量错误 | 调用方持旧 plan dict（自动绑定后未刷新）完成或撤销 | BPC runs 未消耗即释放（"免费"制造）；返还数量失真 | ✅ **已修复** |

---

## 3. 优化建议（P2）

| # | 位置 | 建议 | 状态 |
|---|------|------|------|
| 5 | `services/plan_execution.py:289`、`plan_table.py:652,710`、`tests/test_plan_execution.py:466` 等 | `with direct_connect(...)` 依赖 `sqlite3.Connection` 的上下文协议 —— 只管理事务**不 close**，靠 GC 回收。`database_manager.direct_connect` 文档明确「调用方负责 close」，应改 try/finally 显式关闭 | ✅ **已修复**（plan_execution.py:289 与 plan_table.py:710） |
| 6 | `README.md:292` | 测试总数声明 **727**，实际全量 **777 passed** —— 文档陈旧（0.10.0 文档同步提交 273719c 后仍差 50） | ✅ **已修复**（已同步 783） |
| 7 | `services/init_check.py:86, 229` | `check_implants` 阈值从 >200 降至 >20：仅 32 个工业/发明植入体有 dogma，>20 可能掩盖部分数据缺失（拉取 21 个即判通过，漏 11 个） | ⏳ 未开始 |
| 8 | `services/init_check.py:201-211` | `check_schema` 改为跳过 `get_db_version() is None` 的库：库文件存在但损坏/不可读时被静默跳过，误判 schema 就绪（与 schema_migrations 的表缺失守卫叠加放大） | ⏳ 未开始 |
| 9 | `services/plan_execution.py:289-296` | `complete_plan` 成品数量取 `blueprint_products` 首个匹配行（`LIMIT 1`）+ 活动限定 `manufacturing`：多蓝图/反应配方时数量可能错误，反应类产物 `output_per_run` 恒为 1 | ⏳ 未开始 |
| 10 | `services/plan_execution.py:99-100` | `material_requirements` 对 `runs`/`parallels` 为 `None` 时 `int(None)` 抛 `TypeError`（DB 恒有默认值，实际风险低，但防御不足） | ⏳ 未开始 |
| 11 | `tools/downloaders/sde_cache.py:279-298` | `clear_yaml_cache` 与并发 `load_yaml` 存在 check-then-set 竞态：清缓存瞬间可能在途加载把大文件重新填入 `_YAML_CACHE`（init 流程串行，实际风险低） | ⏳ 未开始 |
| 12 | `tests/` | 覆盖缺口：`complete_plan` 失败/重试路径、`start_plan` 材料部分扣减后的异常回滚、`mat_hangar_id` 不一致（P1-2）、`ensure_sde_cache` / `ensure_universe_cache` 并行解析与 JSON 缓存路径均无测试 | ⏳ 未开始 |
| 13 | `ui_pyside6/theme.py:78-93`（`_StrongCallback`） | lambda 监听器被**强引用**长期持有直至显式 `remove`（文档已注明）。当前代码 0 个 lambda 监听器（plan_table.py:466 唯一一处已正确配对 remove），属潜在陷阱而非现患 | ⏳ 未开始 |
| 14 | `services/plan_execution.py:219-226` | `start_plan` 用 `COALESCE(?, char_name/facility)` 覆盖，但 `mat_hangar_id` 不在其中（同 P1-2 根因）；建议一并纳入持久化并统一 `material_short` 语义 | ⏳ 未开始 |
| 15 | `services/plan_execution.py:370-374` | `cancel_plan` 返还依赖 `material_requirements()` 重算（走评分）：评分失败时返回空列表 → 材料不返还但状态仍翻回 pending（静默材料损失），建议改为记录扣减快照 | ⏳ 未开始 |

---

## 4. 功能缺口清单

| 位置 | 现状 | 应该做什么 | 状态 |
|------|------|-----------|------|
| `plan_execution.cancel_plan` | 返还数量由 `material_short` JSON + 需求重算推导 | 启动时持久化 `deducted_materials` 快照，撤销/异常恢复时精确还原 | ⏳ 未开始 |
| `complete_plan` 事务 | 成品入库/BPC 消耗/状态更新三处非原子 | 统一到单连接事务 + 幂等标记（防重试重复入库） | ✅ **已修复** |
| `start_plan` | 生效机库/自动绑定结果未回写计划记录 | 持久化 `mat_hangar_id` + 自动绑定蓝图后刷新内存 plan | ✅ **已修复**（mat_hangar_id 落库 + `_start_plan` 回写） |
| SDE 缓存 | `ensure_sde_cache` / `ensure_universe_cache` 无自动化测试 | 补 JSON 缓存命中/失效、并行解析一致性的测试 | ⏳ 未开始 |

---

## 5. 已确认无问题的设计（避免重复审计）

- **schema user v3→v4 迁移**（schema_migrations.py）：`DB_SCHEMA_VERSIONS["user"]=4` + `MIGRATIONS["user"][3]=_migrate_user_v3_to_v4` 按规范注册；`_add_columns` 幂等（duplicate 跳过）；全迁移函数新增 `_table_exists` 缺失表守卫；v0 库显式落盘版本号（修复"每次启动弹下载窗"）。测试 `test_user_v3_to_v4_*`（3 例）覆盖列/索引/幂等/缺表。
- **conftest.py 未随 v4 同步**：**非问题**。新迁移测试自带临时 v3 库（`_create_user_v3`），`temp_db`/`db_manager` fixture 不跑 `ensure_schema()`；777 测试全绿实测确认无影响。
- **SDE 并行解析**（sde_cache.py:128-190, 242-244）：`_parse_universe_chunk` 每 worker 独立 `ZipFile` 句柄 + 独立 `CSafeLoader`，`asyncio.to_thread` 切块不重叠（`all_paths[i::workers]`），无共享可变状态，线程安全。
- **`_split_bpc_consumption` 数学**（plan_execution.py:505-523）：quantity/runs/used 全组合（整份/余量坍缩/耗尽删行/零消耗）验证正确，测试 4 例覆盖。
- **ESI 下载器**（getimplantdata.py/getitems.py）：共享 `APIClient`（20 req/s 全局限流 + 429 重试），`BATCH=20/100` + `asyncio.gather` 并发；`fetch` 失败返回 None 被跳过不中断；`typeIDs.yaml` 兼容 int/str 键。
- **初始化流程**（init_service.py / init_check.py）：`get_missing_steps` 单次 `check_all()`、`_net_ok` 每轮缓存、`inspect.signature` 预判 `progress_cb`（避免 except TypeError 兜底导致的双重执行/真实错误掩蔽）、结束 `clear_yaml_cache()` 释放大文件内存。
- **theme lambda 修复**（theme.py:78-93）：`weakref.WeakMethod(lambda)` 确会抛 `TypeError` → `_StrongCallback` 分支生效（已实测验证），lambda 监听器修复有效；当前 0 处未移除的 lambda 监听器。
- **倒计时到期→ready 闭环**：模型 `tick()`（内存）+ `expire_overdue_plans()`（DB 层）+ `load_plans` 首次补算，三层一致。
- **规则合规**：变更文件无裸 `except: pass`；无硬编码 hex 颜色；SQL f-string 全部为「动态占位符 + 参数化值」或内部常量列名（无注入风险）；新代码均带类型注解；`add_theme_listener` 均用绑定方法。

---

## 6. 优先修复路线图

| 优先级 | 项目 | 预计改动文件数 | 说明 |
|--------|------|---------------|------|
| **P1** | 取消路径统一返还（P1-1） | 1 | ✅ **已修复**（按用户设计：新增 `reset_plan_for_reuse`，撤销=in_progress 返还、设为待生产=completed 复用不返还；菜单按状态显隐） |
| **P1** | 持久化 mat_hangar_id（P1-2） | 2 | ✅ **已修复**：start_plan UPDATE 加 `mat_hangar_id=COALESCE(?, mat_hangar_id)` + `_start_plan` 回写内存 dict |
| **P1** | complete_plan 事务化 + 幂等（P1-3） | 2 | ✅ **已修复**：inventory_manager 事务注入 + complete/start/cancel 单事务 + 幂等守卫 |
| **P1** | 权威值重读（P1-4） | 1 | ✅ **已修复**：complete/cancel 重读 DB 的 assigned_blueprint_id / mat_hangar_id / material_short |
| **P2** | direct_connect 显式关闭（P2-5） | 3 | ✅ **已修复**：plan_execution.py:289 / plan_table.py:710 try/finally close |
| **P2** | README 测试数同步（P2-6） | 1 | ✅ **已修复**：727 → 783 |
| **P2** | 扣减快照（P2-15 / 缺口 F1） | 1 | ⏳ 未开始：启动时持久化 deducted_materials，撤销精确还原 |

---

## 附：审计范围与验证记录

- **同步**：`git pull --ff-only` `7c24921..b3b8e40`，工作树干净，HEAD=`b3b8e40`。
- **静态检查**：`uv run ruff check .` → **All checks passed!**（0 错误）；`uv run mypy .` → **Success: no issues found in 187 source files**。
- **全量测试**：`uv run python -m pytest tests/ -q`（不带 maxfail，审计需全量失败信息）→ **777 passed, 34 warnings, 135.94s**。
- **变更集核对**：提交清单与文件清单经 GitHub API `compare/7c24921...b3b8e40` 与本地 `git log` 双重核实，48 文件 +3157/-521 一致。

## 附：修复轮记录（2026-08-01）

**第一轮**：应用户要求修复 P1-2/3/4 与 P2-5/6，改动文件 5 个：

| 文件 | 改动 |
|------|------|
| `services/inventory_manager.py` | `add_item` / `deduct_item` 增加 `conn` 参数（同一连接执行、不提交，由调用方统一事务） |
| `services/plan_execution.py` | `start_plan`：扣减 + 状态更新同一事务，持久化 `mat_hangar_id`；`complete_plan`：单连接事务 + DB 权威值重读 + completed 幂等 + bp 连接显式关闭；`cancel_plan`：DB 权威值重读（mat_hangar_id/material_short/status）+ 返还与状态重置同一事务 |
| `ui_pyside6/views/industry/plan_table.py` | `_start_plan` 回写 `mat_hangar_id`；`_set_status` 改 try/finally 显式关闭连接 |
| `README.md` | 测试总数 727 → 783 |
| `tests/test_plan_execution.py` | 新增 6 例：机库落库、扣减失败回滚、完成失败回滚、幂等不重复入库、stale dict 撤销/完成按 DB 权威值 |

**第二轮（P1-1）**：按用户澄清的领域模型实现取消/复用两条路径，改动 2 个文件 + 测试：

| 文件 | 改动 |
|------|------|
| `services/plan_execution.py` | 新增 `reset_plan_for_reuse(plan_id)`：仅 completed → pending，清 started_at/completed_at/deposited/material_short/蓝图占用，**不返还材料**（材料已变为成品） |
| `ui_pyside6/views/industry/plan_table.py` | 删除 `_cancel_plan` 裸 SQL；新增 `_reset_for_reuse` 调 service 层 + 确认弹窗；右键菜单**按状态显隐**：pending→启动、in_progress→撤销启动（返还）、ready→下线、completed→设为待生产（复用）；撤销确认文案注明"仅游戏未启动时使用" |
| `tests/test_plan_execution.py` | 新增 3 例：completed 复用（元数据清理、库存不动）、非 completed 拒绝、不存在拒绝 |

**领域模型**（用户确认）：
- 撤销启动（返还材料）= 仅**软件误点、游戏未启动**的 in_progress 计划；游戏产线一经启动不退还材料。
- 设为待生产（复用）= 仅 **completed** 计划，材料已变成品、不返还，清状态重跑。
- ready（待下线）= 游戏产线已跑完，仅可「下线」（不可逆产出成品），无撤销。
- 删除行 = 仅释放蓝图不返材料，与游戏"取消产线只退蓝图"行为一致。

- **验证**：`ruff` 0 错误；`mypy` 无问题；目标测试 `test_plan_execution.py` **55 passed**（52 + 3 新增）。全量按工作流约定推迟到提交前跑。
- 其余 P2（7-15）与功能缺口 F1-F4 保持 `⏳ 未开始`，留待后续评估。
