# EVE 工业助手 — 自主开发任务

## 上下文
- 项目: PySide6 桌面应用，Python 3.14
- 架构: `ui_pyside6/views/` (UI) + `services/` (业务逻辑) + `database/` (SQLite, 4库)
- 运行: `venv/Scripts/python Main.py`
- 测试: `pytest`
- Lint: `ruff check .`

## 路线图优先级（参考 EVE——docs/04-开发路线图.md）
1. **P0** 工业制造页增强 — 利润排行列表、Tab 化布局
2. **P0** 评分 UI 展示 — 制造页展示评分 breakdown
3. **P1** 市场贸易页重做 — 从占位到可用的贸易评分 + 价格监控
4. **P1** 多区域价格切换 — 原料/产品独立区域选择
5. **P2** 仓库管理 UI 增强 — 连接 inventory_manager 数据层

## 已完成功能
- 物品搜索（中/英文模糊匹配 + ESI 实时弹窗）
- 工业制造基础（搜索+计算+计划表+材料汇总）
- ME/TE 制造评分修正（浪费因子 + 时间修正 + 利润分解卡片）
- 价格缓存 + 时效检查
- 暗色主题 + 侧边导航 + 工具栏
- 蓝图管理 + 数据库拆分（单库→4库）
- 窗口位置记忆 + 全局异常处理

## 要求
- 每完成一个功能点 → git commit（中文信息）
- 提交前确保 `ruff check .` 通过
- 不改数据库 schema 除非必要
- 不改 `ui_pyside6/theme.py`（主题系统）
- 不改 `Main.py` 或 `dev.py`（入口文件）
- 不引入新依赖（除非功能绝对需要）

## 成功标准
- 每次改动后应用能正常启动
- ruff check 零错误（E501 行长度除外）
- 路线图任务逐步推进
- 全部完成后创建 DONE.md
