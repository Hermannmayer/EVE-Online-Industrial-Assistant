# EVE 商人助手 — 代码审计报告（2026-07-03）

---

## 第一部分：占位函数与未实现功能

共发现 **26 个占位/未实现项**，按模块分组：

### 一、工业页面（industry）— 重灾区

#### 1.1 阶段二占位（5 处）

| # | 文件 | 行号 | 位置 | 当前实现 | 应做什么 |
|---|------|------|------|----------|----------|
| 1 | `industry/plan_table.py` | 188-189 | 右键菜单"查看核算" | `print("查看核算功能暂未实现")` — 用户在 UI 上看不到 | 应打开核算弹窗，展示该行的材料成本/时间/利润 |
| 2 | `industry/plan_table.py` | 191-192 | 右键菜单"查看物品详情" | `print("查看物品详情功能暂未实现")` — 同上 | 应打开物品详情页/弹窗 |
| 3 | `industry_view.py` | 276 | `_on_add_from_text()` | `# 从文本添加计划 — 阶段二占位` + pass | 应解析粘贴的文本（蓝图名/数量），批量创建计划行 |
| 4 | `industry_view.py` | 280 | `_on_add_from_blueprint_list()` | `# 从蓝图列表批量添加 — 阶段二占位` + pass | 应打开蓝图列表选择器，批量添加到计划 |
| 5 | `industry/predefault_dialog.py` | 整个文件 | 窗口标题"预默认配置（阶段一占位，功能开发中）" | UI 表单存在但不保存到数据库 | 应持久化预默认配置到 DB/JSON，新建计划时自动填充 |

#### 1.2 阶段三占位（9 处）

| # | 文件 | 行号 | 位置 | 当前实现 |
|---|------|------|------|----------|
| 6 | `industry/plan_table.py` | 220 | 右键菜单"母项智能调整" | `_phase3_placeholder("母项智能调整")` — 弹提示框 |
| 7 | `industry/plan_table.py` | 222 | 右键菜单"子项智能调整" | `_phase3_placeholder("子项智能调整")` — 弹提示框 |
| 8 | `industry/plan_table.py` | 224 | 右键菜单"子项大规模产线并行" | `_phase3_placeholder("子项大规模产线并行")` — 弹提示框 |
| 9 | `industry/plan_table.py` | 227 | 右键菜单"查看NPC卖家" | `_phase3_placeholder("查看原本图的NPC卖家")` — 弹提示框 |
| 10 | `industry/plan_table.py` | 231 | 右键菜单"设置星系" | `_phase3_placeholder("为设施设置所在星系")` — 弹提示框 |
| 11 | `industry/plan_table.py` | 233 | 右键菜单"设置成本系数" | `_phase3_placeholder("为设施所在星系设置成本系数")` — 弹提示框 |
| 12 | `industry/plan_table.py` | 236 | 右键菜单"产线启动小助手" | `_phase3_placeholder("产线启动小助手")` — 弹提示框 |
| 13 | `industry/plan_table.py` | 188 | 右键菜单"查看核算" | print 到控制台（用户看不到） |
| 14 | `industry/plan_table.py` | 191 | 右键菜单"查看物品详情" | print 到控制台（用户看不到） |

#### 1.3 甘特图占位（1 处）

| # | 文件 | 行号 | 当前实现 |
|---|------|------|----------|
| 15 | `industry/gantt_view.py` | 122 | `paintEvent` 中 `pass` — 甘特图绘制逻辑不完整 |

### 二、库存页面（inventory）

| # | 文件 | 行号 | 位置 | 当前实现 |
|---|------|------|------|----------|
| 16 | `inventory_view.py` | 1885 | 蓝图右键菜单"精炼" | 占位项：暂无操作 |
| 17-19 | `inventory_view.py` | 蓝图右键菜单 ×3 | 其他右键操作 | 全部弹提示框"即将上线" |
| 20-25 | `estimate_view.py` | 精炼栏 ×6 | 精炼相关控件 | 全部 `setEnabled(False)` — 精炼功能完全 disabled |

### 三、其他

| # | 文件 | 说明 |
|---|------|------|
| 26 | `inventory_view.py` | 有 `pass` 异常处理占位（非关键，错误吞没） |

---

## 第二部分：时钟/定时器/调度 功能报告

### 一、定时器清单

| # | 定时器 | 文件 | 间隔 | 用途 | 启停条件 |
|---|--------|------|------|------|----------|
| 1 | `_price_timer` | `main_window.py:843` | 可配（默认30分钟） | 周期触发价格过期检查 → 过期则自动更新 | 自动更新开启且间隔>0时启动；设置页可开关/调间隔 |
| 2 | `_hot_reload_timer` | `main_window.py:234` | 500ms | 轮询 hot reload 状态文件，开发时自动重载 UI | 仅 `--hot-reload` 命令行参数启用时 |
| 3 | `_check_timer` | `watchlist_view.py:415` | 60s | 定时检查关注列表价格变化并刷新 | View 可见时启动，隐藏时停止 |
| 4 | `_debounce_timer` | `query_view.py:721` | 200ms | 搜索输入防抖 | 每次输入文字时 restart |
| 5 | `_debounce` | `all_items_view.py:378` | 200ms | 全物品列表搜索防抖 | 每次输入文字时 restart |
| 6 | `singleShot(2000)` | `Main.py:137` | 一次性 | 启动后延迟 2s 拉取蓝图名称 | 仅启动时 |
| 7 | `singleShot(500)` | `main_window.py:245` | 一次性 | 延迟检测首次运行，触发初始化向导 | 仅 MainWindow 初始化时 |
| 8 | `singleShot(100)` | `main_window.py:249` | 一次性 | 延迟创建系统托盘图标 | 仅 MainWindow 初始化时 |
| 9 | `singleShot(1000)` | `main_window.py:467` | 一次性 | 价格过期后延迟 1s 触发更新 | 自动更新开启且数据过期时 |

### 二、时钟 UI 清单

| UI 元素 | 位置 | 说明 |
|---------|------|------|
| `_price_age_label` ("⏳ 价格: —") | `main_window.py:154` 工具栏 | 显示价格数据距今多久（如"3分钟前"） |
| `_status_label` ("就绪") | `main_window.py:172` 状态栏 | 文本状态：正在更新.../价格过期N分钟/🔔N个物品价格变化 |
| `_status_info_label` | `main_window.py:185` 状态栏右侧 | "价格更新完成"或"价格更新失败: xxx" |
| `_update_progress` (QProgressBar) | `main_window.py:175` 状态栏 | 价格更新时进度条（不确定模式） |
| 工业页 StatusBar | `industry/status_bar.py` | 工业视图专用状态栏（非时钟相关） |

**注意：没有实时时钟显示，没有倒计时，没有"下次更新倒计时"。**

### 三、未实现/可添加的定时功能

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 下次价格更新倒计时 | 中 | 状态栏显示"下次更新: 12:34"，用户感知节奏 |
| 手动刷新按钮倒计时 | 低 | 点击"更新价格"后按钮变灰 + 倒计时 |
| EVE 游戏时间显示 | 低 | EVE 内时间对商人有参考价值（Jita时间） |
| ESI 速率限制倒计时 | 低 | 当前无 ESI rate limit 可视化 |

---

## 总结

### 按优先级排序

**P0（用户可见的功能缺失）：**
- 工业页右键菜单 7 个功能点了没反应（弹提示框）
- 添加计划 / 批量添加完全为空
- 蓝图右键 4 个菜单项全部"即将上线"
- 查看核算/查看物品详情 print 到控制台（用户看不到）

**P1（功能不完整）：**
- 预默认配置不持久化
- 精炼功能 6 个控件全部 disabled
- 甘特图绘制不完整
- 没有价格更新倒计时

**P2（锦上添花）：**
- EVE 游戏时间显示
- ESI 速率限制可视化
