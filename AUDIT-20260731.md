# EVE 商人助手 审计报告

> 审计日期: 2026-07-31 | 审计依据: `AI-审计任务书-20260731.md`
> 代码基线: 0.7.0（含本次修复提交 79e9128 / 71006bf / 79b93d2）
> 范围: 设计架构 / 流程 / 设计模式 / 功能完整性 / Bug / 测试 / 性能 / 数据一致性

---

## 0. 总评

项目整体架构健康：三层分离（core → services → ui_pyside6）基本成立、DB 连接按线程隔离设计正确、测试全部 mock 无真实网络依赖、崩溃转储与占位机制兜底良好。但审计发现 **1 个 P0 数据错误（精炼功能恒为空）、2 个 P0 迁移缺陷（半途崩溃数据撕裂）和 1 个 P0 公式疑点（材料浪费，用户搁置待验证）**；另有 14 项 P1 问题——其中 **11 项已在本轮修复中解决**（评分缓存死代码、ESI 限流、费用口径、save_prices 数据清空、主题监听器泄漏、主线程大表查询等），3 项待后续（BOM 环检测、公式核对、迁移标记的存量库兼容）。最优先的剩余动作：**核对材料浪费公式的现行机制**（影响所有制造利润的准确性）。

---

## 1. 严重问题（P0）

| # | 位置 | 问题 | 触发场景 | 影响 | 状态 |
|---|------|------|---------|------|------|
| 1 | `services/scoring_service.py:370`、`services/refining_service.py:39`、`ui_pyside6/views/estimate_view.py:866` | 精炼查 `type_materials` 表，实际表名 `reprocessing_materials`（reference.db 实测无 type_materials） | 任何精炼计算 | 精炼价值恒为空，功能完全失效 | ✅ **已修复**（改表名 + 统一 RefiningService + test_refining_service.py） |
| 2 | `Main.py:42-81`（原） | `_migrate_blueprint_db` 非原子：CREATE AS SELECT + DROP 循环中途崩溃 → ref/bp 两库各持一半表，重启判定仅文件存在即跳过 | 迁移中途断电/崩溃 | 蓝图数据永久撕裂 | ✅ **已修复**（.tmp + os.replace 原子替换 + test_migration_recovery.py） |
| 3 | `Main.py:28-29`（原） | `_migrate_split_db` 以「三库文件存在」判定完成，半迁移库（user.db 半表）被跳过 | 迁移中断后重启 | user 数据丢失不可自愈 | ✅ **已修复**（`_split_migration_complete` 完成标记 + 幂等重跑） |
| 4 | `services/manufacturing_calculator.py:58-104` | **材料浪费公式存疑**：线性近似 `(100-ME)/100`，wastefactor 被显式忽略（:79）；`_waste_mult`（:51-55）正确的旧公式被弃用 | 所有制造利润计算 | ME0 时 T1 少算 10% 浪费 → 利润系统性虚高 | ⏸️ **用户搁置，待验证**（需对照现行 EVE 机制再定公式） |

---

## 2. 一般问题（P1）

| # | 位置 | 问题 | 状态 |
|---|------|------|------|
| 5 | `services/scoring_service.py` 缓存区（原） | 评分缓存死代码：`get_cache/set_cache/_default_cache` 零调用，`ScoringService._cache` 存而不用；两套缓存实现重复（ScoringCache vs TtlLRUCache） | ✅ **已修复**（接线 + 删重复实现 + 价格刷新失效联动 + 修复 `__len__` 真值陷阱） |
| 6 | `services/workers/getprices.py:250-255`（原） | `save_prices` 先 DELETE 后 INSERT：拉取失败 → 区域价格被清空 | ✅ **已修复**（失败区域跳过删除保留旧价） |
| 7 | `ui_pyside6/main_window.py:609-614`（原） | 缩进 bug：「暂无数据」在 except 块外 → 价格年龄标签永远显示"暂无数据" | ✅ **已修复**（移入正确分支） |
| 8 | `services/scoring_service.py:794-797, 937-943` | 费用口径：改单 broker 按全额 revenue 计，改单场景重复收费 | ✅ **已修复**（差额计费，制造+贸易两路径） |
| 9 | `services/database_manager.py:31-49` | 无 `busy_timeout`：aiosqlite worker 与主线程 sqlite3 双连接并发写读可能 locked | ✅ **已修复**（4 库 + direct_connect 统一 30s） |
| 10 | `services/workers/getprices.py:98-118` | ESI 限流不足：Semaphore(50) 形同虚设、无主动限速、非 200 静默 `return []`、`fetch_baseline_prices` 无超时 | ✅ **已修复**（RateLimiter 令牌桶 20 req/s + 统一 APIClient + 失败记日志） |
| 11 | `services/scoring_service.py:570-571`（原） | `calculate_plan_metrics` 异常吞错 → 利润全 0 静默显示 | ✅ **已修复**（log.exception） |
| 12 | `services/database_manager.py:189-229`（原） | 三个死代码方法引用不存在的表/列（`i.name`、`bp.blueprints`） | ✅ **已修复**（删除，用户已确认） |
| 13 | `core/container.py:189`、`services/scoring_service.py:157`（原） | services 反向 import UI 层（char_settings_view）形成环 | ✅ **已修复**（下沉到 services/char_config_resolver） |
| 14 | `ui_pyside6/theme.py` 监听器 | ~20 处 `add_theme_listener` 无对应 remove → 页面销毁后泄漏 | ✅ **已修复**（weakref.WeakMethod 基础设施，一处改全受益） |
| 15 | `ui_pyside6/main_window.py:581-585` | 主线程 `MAX(fetch_time)` 全表扫描（10 万+ 行） | ✅ **已修复**（fetch_time 索引，schema_migrations mkt v2→v3） |
| 16 | `services/schema_migrations.py:190-193`（原） | v0 判定：有表但 user_version=0 直接标 v1 跳过 → 缺列崩溃 | ✅ **已修复**（v0 补跑全部幂等迁移） |
| 17 | `services/production_scheduler.py` | 427 行排产调度器零调用（死代码） | ✅ **已修复**（删除，用户已确认） |
| 18 | `services/bom_expander.py:131-142,228` | BOM 环检测缺陷：真环被 max_depth 截断后当市场价叶子计价，静默错价 | ⏳ **待处理**（需显式环检测 → 标记"环-无法计算"） |
| 19 | `services/scoring_service.py:546` | `facility_tax` 缺省 0 时安装费少计（NPC 0.25%） | ⏳ **待处理**（角色配置无字段时用 FACILITY_TAX_NPC） |
| 20 | 迁移标记兼容 | `_split_migration_complete` 新表：已拆分完成的存量库无标记 → 下次启动会重跑迁移（幂等，无数据损坏但浪费时间） | ⏳ **待处理**（一次性兼容：检测 4 表齐全则补写标记） |

---

## 3. 优化建议（P2）

| # | 位置 | 建议 | 状态 |
|---|------|------|------|
| 21 | `services/scoring_service.py:48` 等 6 文件 | 模块级 `db = get_db()` → 函数内惰性获取 | ⏳ 未开始 |
| 22 | `services/scoring_service.py:34-40`、`pricing_service.py:11-17` | `_TRADE_HUB_SYSTEM_IDS` 三处重复 → 合并到 constants | ⏳ 未开始 |
| 23 | `ui_pyside6/views/settings_view.py:154-168` | 设置对话框「默认参数」页 4 控件创建但不读写 | ⏳ 未开始 |
| 24 | `core/single_instance.py:32-34` | 锁文件 check-then-act 竞态 → O_CREAT\|O_EXCL 原子化 | ⏳ 未开始 |
| 25 | `services/workers/getcontracts.py:186`、`Main.py:92` | `datetime.utcnow()` 弃用 API；崩溃文件名本地时间 | ⏳ 未开始 |
| 26 | `tests/` | `--quick` marker 名不副实（仅 8 文件标 slow）；`ui` marker 死注册 | ⏳ 未开始 |
| 27 | `services/workers/getprices.py:28` | `_PAGE_CACHE` 无 TTL（页数过期） | ⏳ 未开始 |
| 28 | `services/workers/getprices.py:213-240` | `market_volume_snapshots` 无限累积无清理 | ⏳ 未开始 |
| 29 | `services/bom_expander.py:145-160` | BOM 中间产品无 memoization（指数风险靠 max_depth 兜底） | ⏳ 未开始 |
| 30 | `services/client.py` | HTTP 三套实现（APIClient / getprices 裸 aiohttp / price_history 内联）→ getcontracts/getitems 也迁移到 APIClient | ⏳ 未开始 |
| 31 | `services/repositories/` | UI 直接 SQL（28 处 direct_connect + 60+ connect）绕过 repositories 层 | ⏳ 未开始 |

---

## 4. 功能缺口清单

| 位置 | 现状 | 应该做什么 | 状态 |
|------|------|-----------|------|
| 精炼功能（F1） | 查错表恒为空 | 改 `reprocessing_materials` | ✅ 已修复 |
| 评分缓存（F3） | 死代码 | 接线 + 失效联动 | ✅ 已修复 |
| `services/production_scheduler.py` | 排产调度器零调用 | 删除 | ✅ 已修复 |
| `manufacturing_calculator` 测试 | 公式 205 行无直接测试 | 新建 test_manufacturing_calculator.py（排除搁置的浪费公式） | ⏳ 未开始 |
| `init_service.py` / `plan_aggregator.py` / `blueprint_reader.py` / `char_config_resolver.py` | 无测试 | 补测试 | ⏳ 未开始 |
| qtbot 交互测试 | 全 tests 零处 qtbot 用法 | 引入信号/点击级测试 | ⏳ 未开始 |

---

## 5. 已确认无问题的设计（避免重复审计）

- **DB 连接线程隔离**：`threading.local` + `check_same_thread` 兜底，主线程与 QThread worker 各持独立连接，不会跨线程共用（database_manager.py:60-69）
- **QTimer 生命周期**：watchlist 60s 定时器 show/hide stop-start 配对正确；主窗口价格定时器先 stop 再重建；无真正泄漏（依赖 Qt 父子析构，模式脆弱但正确）
- **单实例锁**：PID 存活检测跨平台设计正确（Windows ctypes / POSIX os.kill）；锁文件崩溃残留靠 PID 检测兜底
- **时区处理主体正确**：数据库 UTC 存储（`datetime('now')`）+ 显示层 +8 北京时区；`_FP_EPSILON` 浮点补偿方向正确
- **公式中安装费/税/时间**：对照现行 EVE 机制结构正确（`EIV×(SCI×SB+FT+SCC+AT)`、经纪人费指数公式、时间乘法叠加）——**材料浪费公式除外（P0-4 待验证）**
- **测试隔离**：全部 mock aiohttp 与真实 DB，无真实网络依赖；temp_db 用临时目录
- **无完全裸 `except:`**（全部 `except Exception` 级别）
- **崩溃转储 excepthook** 健全（crash 文件 + 弹窗）；占位机制（placeholder 页 + InitWizard）兜底良好
- **无模块级 `get_container()` 调用**（容器仅在方法体内使用，线程安全有锁）

---

## 6. 优先修复路线图

| 优先级 | 项目 | 预计改动文件数 | 说明 |
|--------|------|---------------|------|
| **P0** | 材料浪费公式核对（P0-4） | 1-2 | 需先对照 EVE 现行机制确认公式，再改 manufacturing_calculator + 测试 |
| **P1** | BOM 环检测（P1-18） | 1 | bom_expander.py：路径集合检测 → 标记"环-无法计算" |
| **P1** | facility_tax 缺省（P1-19） | 1 | 角色配置无字段时用 `FACILITY_TAX_NPC` |
| **P1** | 迁移标记存量兼容（P1-20） | 2 | Main.py + migrate_split_db：表齐全则补写标记 |
| **P2** | 模块级 get_db 惰性化（P2-21） | 6 | 与 P1-13 联动已完成部分 |
| **P2** | 常量合并（P2-22） | 3 | 三处 hub 映射 → constants |
| **P2** | 测试补缺（F24） | 5+ | manufacturing_calculator / init_service / plan_aggregator / blueprint_reader / repositories |

---

## 附：审计范围与已修复提交

- **已修复提交**：`79e9128`（P0：精炼/迁移/char_config 下沉）、`71006bf`（P1×10：缓存/限流/差额计费/busy_timeout 等）、`79b93d2`（P1 尾：theme weakref / 索引 / 迁移收敛 / 删 scheduler）
- **新增测试**：test_refining_service（4）、test_migration_recovery（4）、test_schema_migrations（4）、test_scoring_cache 扩展（+6）、test_getprices 扩展（+3）、test_client 扩展（+3）、test_database_manager（+3）、test_theme_listeners（+2）
- **材料浪费公式（P0-4）**：标注「待验证」——`manufacturing_calculator.py:51-55` 保留的 `_waste_mult` 是候选正确公式（`1 + wf/(100×(1+ME))`），确认机制后切换
