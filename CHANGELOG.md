# 更新日志

## [未发布] — 2026-06-30

### 新增
- **亮色主题**：新增 One Light 主题，支持运行时一键切换
- **主题持久化**：主题偏好自动保存到 settings.json，重启保留
- **系统设置对话框**：将系统设置从弹出菜单改为独立对话框，整合主题、价格更新间隔、自动更新开关
- **价格自动更新开关**：可启用/禁用周期性价格更新
- **周期性价格检查**：新增 QTimer 每 N 分钟自动检查价格时效

### 修复
- **启动时忽略自动更新设置**：_on_price_check_done 在启动时无条件触发价格更新，未检查 uto_update_enabled 设置。现在只有当自动更新启用且价格过期时才会自动更新，关闭时仅提示

- **亮色模式硬编码问题**：全物品弹窗、右键菜单、二级菜单、弹窗中所有硬编码颜色替换为 theme 常量，确保主题切换时正确跟随
- **价格更新间隔不生效**：改为 QTimer 周期性触发，修改间隔后即时重启定时器
- **设置保存失败静默**：加载/保存设置异常不再静默吞掉，改为状态栏提示
- **InitWizard 重复打开崩溃**：关闭后 C++ 对象被释放，加 RuntimeError 保护
- **全物品弹窗主题不一致**：AllItemsDialog 注册主题监听器，切换主题时刷新所有内联样式

### 规范
- **CLAUDE.md 配色铁律**：新增「配色铁律」章节，规定所有颜色必须从 theme 导入，禁止 hex/rgb/颜色名，每次会话自动加载
- **theme.py 工厂函数**：新增 `themed_menu()` 工厂，降低正确做法成本
- **右键菜单统一化**：移除所有 context menu 的 inline setStyleSheet，统一走全局 QSS
